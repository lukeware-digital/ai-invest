# ==========================================
# CeciAI - Requirements
# Python 3.11 | Alta Performance | Baixo Custo
# ==========================================

# ===== Core (Essencial) =====
python-dotenv==1.0.0
requests==2.31.0
aiohttp==3.9.1              # HTTP assíncrono
httpx==0.25.2               # HTTP client moderno

# ===== Database (Zero Custo, Alta Performance) =====
duckdb==0.9.2               # Banco principal (100x mais rápido que SQLite)
redis==5.0.1                # Cache em memória
redis[hiredis]==5.0.1       # Redis com parser C (mais rápido)

# ===== Data Processing =====
pandas==2.1.4
numpy==1.26.2
pyarrow==14.0.1             # Parquet (compressão eficiente)

# ===== API (Assíncrono) =====
fastapi==0.108.0            # Framework web assíncrono
uvicorn[standard]==0.25.0   # ASGI server (com uvloop para performance)
pydantic==2.5.3             # Validação de dados
pydantic-settings==2.1.0    # Settings management

# ===== Ollama / LLM =====
ollama==0.1.6               # Cliente Ollama (LLM local gratuito)

# ===== Machine Learning (GPU Otimizado) =====
torch==2.1.2                # PyTorch (suporte CUDA)
scikit-learn==1.3.2         # ML clássico
xgboost==2.0.3              # Gradient boosting
lightgbm==4.1.0             # Light GBM (rápido e leve)

# ===== Technical Analysis =====
# pandas-ta==0.3.14b0       # Indicadores técnicos (mais leve que TA-Lib) - versão não disponível
# Usando implementação customizada em utils/technical_indicators.py

# ===== Async & Performance =====
asyncio==3.4.3
uvloop==0.19.0              # Event loop mais rápido
aiocache==0.12.2            # Cache assíncrono

# ===== Utilities =====
python-dateutil==2.8.2
pytz==2023.3
orjson==3.9.10              # JSON ultra-rápido (em C)
msgpack==1.0.7              # Serialização binária eficiente

# ===== Monitoring & Logging =====
loguru==0.7.2               # Logging avançado
prometheus-client==0.19.0   # Métricas

# ===== Testing =====
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1
pytest-mock==3.12.0
httpx==0.25.2              # Já listado acima, necessário para TestClient

# ===== Development Tools =====
ruff==0.1.15                # Formatação e linting ultra-rápido (substitui Black + isort + flake8)
mypy==1.7.1                 # Type checking
ipython==8.19.0             # Shell interativo

# ===== Security =====
cryptography==41.0.7        # Criptografia

# ===== Optional (Comentar se não usar) =====
# tensorflow==2.15.0        # Alternativa ao PyTorch (pesado)
# ta-lib==0.4.28            # Indicadores técnicos (requer compilação)
# backtrader==1.9.78.123    # Backtesting (pesado)
# plotly==5.18.0            # Visualização (pesado)
# dash==2.14.2              # Dashboard (pesado)

# ===== Notes =====
# - Instalar Ollama separadamente: https://ollama.ai/download
# - Para GPU: pip install torch --index-url https://download.pytorch.org/whl/cu118
# - DuckDB: Zero configuração, zero custo, alta performance
# - Redis: Opcional, mas recomendado para cache